export default function AdminNewPostPage() {
  return (
    <div className="rounded-[2rem] bg-white p-6 shadow-panel">
      <h1 className="font-display text-3xl font-semibold">Nuevo post</h1>
      <p className="mt-3 text-[hsl(var(--foreground))/0.72]">Formulario reservado para `POST /posts`.</p>
    </div>
  )
}

