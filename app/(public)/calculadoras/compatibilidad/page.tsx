export default function CompatibilityPage() {
  return (
    <div className="mx-auto max-w-4xl px-6 py-12">
      <h1 className="font-display text-4xl font-semibold">Calculadora de compatibilidad</h1>
      <p className="mt-4 text-[hsl(var(--foreground))/0.72]">
        Ruta lista para agregar lógica en `lib/numerology/compatibility.ts`.
      </p>
    </div>
  )
}
